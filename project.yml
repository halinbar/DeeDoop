name: DeeDoop
options:
  bundleIdPrefix: io.deedoop
  deploymentTarget:
    iOS: "16.0"
targets:
  DeeDoop:
    type: application
    platform: iOS
    sources:
      - DeeDoop
    settings:
      base:
        INFOPLIST_FILE: DeeDoop/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: io.deedoop.app
        TARGETED_DEVICE_FAMILY: 1,2
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
