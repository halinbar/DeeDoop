
In Progress



To Do
- fix counters, or remove them
- instead of test- on the top of the picture show a favorites icon
- fix crop
- fix rotate
- add clip embedding to the app.
--- start using it to delete utility photos
- sync with google photos, google drive, icloud.


Backlog
- photos received in whatsapp
- identify utility photos - select which to keep and delete them
- identify blurry photos - select if to delete them or not || iOS already provides a built-in blur / sharpness signal through the Vision framework
- identify dark photos - select if to delete them or not
- identify unhappy photos - select if to delete them or not
- show contacts with the same number. select if to unify them or not, and of so which should be deleted and which shuold be kept, and which fields shuold be transferred from the one to delete to the one to keep
- find contacts with no last name and porpose a reotganized name for them


Done
- add swipe up to save as a favorite
- sync with existing albums and manage them through the app
- Find and delte duplicated photos
- Find and delte duplicated videos
- Find duplicated files and copy their names
- show identical contacts (by name or by contact details) and choose which to keep 
- show bursts and allow selecting one (or multiple) photos out of a burst to save - and delete the rest.
photo bursts are photos taken very closely to each other- a few seconds apart
- show videos by size and select if to delete them or not
- show files by size and select if to delete them or not.

